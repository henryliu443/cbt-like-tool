# Progress

Last visited: 2026-06-15T13:04:40Z

Completed:
1. Fixed memory leak in `ExerciseSessionView.swift`.
2. Extracted `ExerciseAttributes.swift` from `ExerciseLiveActivity.swift`.
3. Created `LiveActivityManager.swift`.
4. Updated `.pbxproj` using Ruby script to add the new files to the correct targets.
5. Integrated `LiveActivityManager` into `ExerciseSessionView.swift` and `ObservableExerciseSession`.
6. Added `NSSupportsLiveActivities` to `Info.plist`.

Waiting for `xcodebuild` to finish.
